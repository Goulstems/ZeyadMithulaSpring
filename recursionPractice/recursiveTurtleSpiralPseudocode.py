def draw_spiral(turtle, length):
    # [Base case: stop when length is very small]
    if length <= 5:
        return

    # [Draw one segment of the spiral]
        #move turtle forward by length
        #turn turtle right by 30 degrees

    # [Recursive step: call the function again, with DECREMENTED length!]
        #draw_spiral(turtle, ????)